// FineInvoice entitlement gate v3
(() => { 'use strict'; const invNo=()=>{const m=String(document.getElementById('invNumber')?.value||'').match(/(?:INV[-_ ]?)(\d+)/i);return m?Number(m[1]):null}; const free=()=>{const n=invNo();return Number.isFinite(n)&&n>=1&&n<=4}; const id=()=>typeof currentDraftId!=='undefined'&&currentDraftId!=null?String(currentDraftId):String(new URLSearchParams(location.search).get('invoice')||document.getElementById('invNumber')?.value||'draft'); const open=(u,x)=>free()||String(u?.plan||'').toLowerCase()==='lifetime'||(u?.unlockedInvoiceIds||[]).map(String).includes(String(x)); async function user(){const s=getSupabase();if(!s)return getCurrentUser?.()||null;let r=await s.auth.getSession();if(!r.data?.session)r=await s.auth.refreshSession();const a=r.data?.session?.user;if(!a)return null;const m=a.user_metadata||{},p=getCurrentUser?.()||{},pl=['single','lifetime'].includes(String(m.plan??p.plan??'free').toLowerCase())?String(m.plan??p.plan).toLowerCase():'free',u={...p,id:a.id,email:a.email||p.email||'',name:m.name||p.name||a.email||'User',plan:pl,planVerified:m.planVerified===true,paymentProvider:m.paymentProvider||p.paymentProvider||null,freePdfCredits:Math.max(0,Number(m.freePdfCredits??p.freePdfCredits??0)||0),paidSingleCredits:pl==='single'?Math.max(0,Number(m.paidSingleCredits??p.paidSingleCredits??0)||0):0,unlockedInvoiceIds:Array.isArray(m.unlockedInvoiceIds)?m.unlockedInvoiceIds.map(String):(p.unlockedInvoiceIds||[])};u.singleCredits=u.freePdfCredits+u.paidSingleCredits;saveCurrentUser(u);return u} async function consume(u,x){if(open(u,x))return{ok:true};const n=Math.max(0,Number(u.paidSingleCredits||0));if(n<=0)return{ok:false,error:'No PDF credits remaining.'};const s=getSupabase(),ids=[...new Set([...(u.unlockedInvoiceIds||[]).map(String),String(x)])];if(!s)return{ok:false,error:'Authentication service is unavailable.'};const r=await s.auth.updateUser({data:{freePdfCredits:Number(u.freePdfCredits||0),paidSingleCredits:n-1,singleCredits:Number(u.freePdfCredits||0)+n-1,unlockedInvoiceIds:ids}});if(r.error)return{ok:false,error:r.error.message};u.paidSingleCredits=n-1;u.singleCredits=Number(u.freePdfCredits||0)+n-1;u.unlockedInvoiceIds=ids;saveCurrentUser(u);return{ok:true}} function install(){window.downloadPDF=async()=>{const u=await user();if(!u){showToast('Please sign in again.','error');return}const x=id(),need=!open(u,x)&&!free()&&u.plan!=='lifetime';if(need&&Number(u.paidSingleCredits||0)<=0){showToast('No PDF credits remaining. Please purchase a PDF credit or Lifetime Access.','error',5000);return}try{const p=document.querySelector('#invoiceDoc .invoice-paper');if(!p)throw Error('Invoice is empty');const c=await html2canvas(p,{scale:3,useCORS:true,backgroundColor:'#fff'}),{jsPDF}=window.jspdf,w=210,m=6,cw=w-m*2,mm=cw/c.width,ch=297-m*2,pages=Math.max(1,Math.ceil(c.height*mm/ch)),d=new jsPDF({orientation:'portrait',unit:'mm',format:'a4'});for(let i=0;i<pages;i++){if(i)d.addPage();const sy=Math.floor(i*ch/mm),sh=Math.min(c.height-sy,Math.ceil(ch/mm)),z=document.createElement('canvas');z.width=c.width;z.height=sh;z.getContext('2d').drawImage(c,0,sy,c.width,sh,0,0,c.width,sh);d.addImage(z.toDataURL('image/jpeg',.96),'JPEG',m,m,cw,sh*mm)}if(need){const q=await consume(u,x);if(!q.ok)throw Error(q.error)}d.save((document.getElementById('invNumber')?.value||'invoice').replace(/[^a-z0-9._-]/gi,'_')+'.pdf');showToast('PDF downloaded! 🎉','success')}catch(e){console.error(e);showToast(e.message||'PDF failed.','error',5000)}};window.printInvoice=async()=>{const u=await user();if(!u)return;const x=id(),need=!open(u,x)&&!free()&&u.plan!=='lifetime';if(need){const q=await consume(u,x);if(!q.ok){showToast(q.error,'error');return}}document.body.classList.add('print-mode');window.print();setTimeout(()=>document.body.classList.remove('print-mode'),800)}} window.addEventListener('load',install,{once:true}); })();